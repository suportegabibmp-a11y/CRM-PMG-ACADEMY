{
  "name": "cardapio-digital",
  "version": "1.0.0",
  "private": true,
  "description": "SaaS de cardápio digital com pedidos e pagamento online (PIX e cartão via Mercado Pago)",
  "main": "server/index.js",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "seed": "node server/seed.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "postgres": "^3.4.9",
    "serverless-http": "^4.0.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.5.8"
  }
}
